---
title: "tideman"
output:
  html_document: default
  pdf_document: 
    latex_engine: xelatex
---

```{r}
library(consensus)
```


```{r}
(por01)
(votrix(por01))
tideman(por01)
```
```{r}
(por02)
(votrix(por02))
tideman(por02)
```


```{r}
(por03)
(votrix(por03))
tideman(por03)
```

```{r}
(por04)
(votrix(por04))
tideman(por04)
```

```{r}
(por05)
(votrix(por05))
tideman(por05)
```

```{r}
(por06)
(votrix(por06))
tideman(por06)
```

```{r}
(por07)
(votrix(por07))
tideman(por07)
```

```{r}
(por08)
(votrix(por08))
tideman(por08)
```

```{r}
(por09)
(votrix(por09))
tideman(por09)
```

```{r}
(por10)
(votrix(por10))
tideman(por10)
```

```{r}
(por11)
(votrix(por11))
tideman(por11)
```

```{r}
(por12)
(votrix(por12))
tideman(por12)
```

```{r}
(por13)
(votrix(por13))
tideman(por13)
```

```{r}
(por14)
(votrix(por14))
tideman(por14)
```

```{r}
(por15)
(votrix(por15))
tideman(por15)
```
