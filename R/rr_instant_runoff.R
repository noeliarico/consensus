instant_runoff <- function(profileOfRankings) {

  # candidates <- profileOfRankings$candidates
  # por <- profileOfRankings$profileOfRankings
  # 
  # for(i in 1:length(candidates)) {
  #   values <- colSums(por == i)
  #   for(j in 0:max(values)) {
  #     
  #   }
  # }
}