match.fun("mean")
my_function <- function(x) x + 3
match.fun("my_function")
Use lapply() and an anonymous function to find the coefficient of variation (the standard deviation divided by the mean) for all columns in the mtcars dataset.power <- function(exponent) {
function(x) {
x ^ exponent
}
}
power <- function(exponent) {
function(x) {
x ^ exponent
}
}
square <- power(2)
square(4)
square
??pryr
install.packages("pryr")
library(pryr)
cube <- power(3)
square
cube
power
square <- power(2)
cube <- power(3)
square
cube
power
power(5)
evironment(square)
library("pryr")
evironment(square)
environment(square)
as.list(environment(square))
unenclose(square)
